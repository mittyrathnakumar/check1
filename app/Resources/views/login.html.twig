{% block login %}
	{#
		{% if error %}
		    <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
		{% endif %}
	#}
	<form class="form-signin" id="frmLogin" method="post" action="{# {{ path('Login/CheckUserLogin') }} #}">
        <h2 class="form-signin-heading">Please sign in</h2>                
        <input type="email" id="Email" class="form-control" placeholder="Email address" required autofocus>
        <label for="inputPassword" class="sr-only">Password</label>
        <input type="password" id="Password" class="form-control" placeholder="Password" required>
        <button class="btn btn-lg btn-primary btn-block" id="loginsubmit" type="submit">Sign in</button>
	</form>	
{% endblock %}
 <div id="dialog" title="Result Action" class="modal_dialog"></div>

